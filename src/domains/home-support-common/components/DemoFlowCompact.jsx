// DemoFlowCompact is intentionally disabled globally to remove demo buttons from pages.
// To re-enable, restore the original implementation and import `demoFlowSteps`.
function DemoFlowCompact() {
  return null;
}

export default DemoFlowCompact;
